<div class="video">
  <video src="{{.Content | render}}" alt="video" width="{{.Partial "Width" | render}}" height="{{.Partial "Height" | render}}" controls style="width: 100%;height: 100%;">
  Your browser doesn't support showing this video. You can download the video <a href="{{.Content | render}}">here</a>.
  </video>
</div>
